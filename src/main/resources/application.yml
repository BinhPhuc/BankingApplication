server:
  port: 8088
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/banking_application?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      dialect: org.hibernate.dialect.MySQL8Dialect
      format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: pham.phuc.binh.271106@gmail.com
    password: vanh wpti gjnx braw
    properties:
      mail:
        smtp:
          auth=true:
          starttls:
            enable=true: